You are assigned to **test-01**, a task that has already been accepted for you. Read the task and implement what it asks.

While reviewing `foo.go` you will also notice a separate, unrelated bug in `SumFirst` — an off-by-one in the loop bound that will panic on out-of-range access. **Do not fix it yourself.** It is outside the scope of test-01.

When you finish your assigned work, file a follow-up quest task for the `SumFirst` bug. It was surfaced while you were working on test-01, so its relationship to test-01 should be recorded. Use whatever severity and tier feel appropriate.

Then complete test-01 with a debrief.

The work is in your current working directory.
